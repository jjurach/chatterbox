"""
Application entry point for the voice assistant server.

The core agent logic and protocol adapters have been moved to the `cackle/`
library package for better reusability and separation of concerns. This
directory now only contains the application-specific entry point (main.py).

See `cackle/` for the core agent and adapter implementations.
"""

__version__ = "0.1.0"
